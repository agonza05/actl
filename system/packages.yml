tasks:
  update:
    aliases: [up]
    cmds:
      - brew update && brew upgrade && brew cleanup
    desc: Update packages.
    platforms: [darwin]
    preconditions:
      - type brew
    silent: true

version: 3
